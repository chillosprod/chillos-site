# Google Sans font slot

The site is configured to load the default interface font from:

`assets/fonts/GoogleSans.ttf`

The font file is intentionally not included in this package. Place a properly licensed copy at that path before publishing, or leave the file absent to use the system fallback stack.

Official specimen page:
https://fonts.google.com/specimen/Google+Sans
